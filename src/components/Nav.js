import React from 'react';
import Navbar from 'react-bootstrap/Navbar';

export default function Nav() {
    return (
        <Navbar bg="dark" variant="dark">
            <Navbar.Brand>React Employee Tracker</Navbar.Brand>
        </Navbar>
    )
};